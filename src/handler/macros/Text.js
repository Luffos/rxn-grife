import React from "react";
import { Text } from "react-native";
import { Styled } from "../Styled";
class _Text extends React.Component {
}
export const StyledFromText = (callback) => Styled(Text, callback);
