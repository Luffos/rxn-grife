import React from "react";
import { View } from "react-native";
import { Styled } from "../Styled";
class _View extends React.Component {
}
export const StyledFromView = (callback) => Styled(View, callback);
